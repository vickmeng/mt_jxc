import * as React from 'react';
import {TransferContainer} from '../components/Transfer';

interface Props {
}
  
export class TransferView extends React.Component<Props> {
    render() {
        return <TransferContainer/>
    }
}